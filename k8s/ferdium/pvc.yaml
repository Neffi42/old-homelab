apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: ferdium
  name: ferdium
  namespace: ferdium
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
